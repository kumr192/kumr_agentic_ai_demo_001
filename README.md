# agentic-tool-demo (BYOK)

A minimal Streamlit app that demonstrates OpenAI tool calling ("agentic" behavior).

## How to run locally
```bash
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
